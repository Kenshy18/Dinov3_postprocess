# Compatibility UI Entrypoint

The Qt UI implementation lives in `apps/qt_ui`.

Use either entrypoint:

```bash
apps/qt_ui/run_app.sh
UI/run_app.sh
```

`UI/run_app.sh` is kept so existing shortcuts and notes continue to work.
