"""Backend-facing detector adapters.

Heavy model runtimes live under backend/detectors/<detector>/runtime. Backend
code should call the adapters here rather than constructing runtime commands
directly.
"""
