# Compatibility Entry

The DINOv3 runtime implementation now lives in:

```text
backend/detectors/dinov3/runtime
```

Files in this directory are compatibility wrappers for existing commands and
old references.
